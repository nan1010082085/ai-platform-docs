{
  "name": "schema-platform-docs",
  "version": "1.0.0",
  "description": "Documentation for Schema Platform",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bash sync-project-docs.sh && vitepress dev",
    "build": "bash sync-project-docs.sh && vitepress build",
    "preview": "vitepress preview",
    "deploy": "npm run build && gh-pages -d .vitepress/dist"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vitepress": "^1.0.0"
  },
  "dependencies": {
    "mermaid": "^11.16.0",
    "vitepress-plugin-mermaid": "^2.0.17"
  }
}
